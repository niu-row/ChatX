{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local ChatX desktop console.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
